<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartdispenserv1">

    <application
        android:name=".NotificationChannelCap"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".admin.RegisterAdmin"/>
        <activity android:name=".admin.AdminMainActivity" android:noHistory="true" android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".admin.AdminLoginActivity" android:noHistory="true" />
        <activity android:name=".User.RegisterUser" />
        <activity android:name=".User.UserLoginActivity" android:noHistory="true"/>
        <activity android:name=".LoginMenuActivity"  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User.UserMainActivity"  android:noHistory="true"/>
    </application>

</manifest>